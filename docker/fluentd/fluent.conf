<source>
  @type forward
  port 24224
  bind 0.0.0.0
  log_level trace
</source>
<match *.**>
  @type file
  path /fluentd/log/file
  <buffer>
     @type file
     path /fluentd/log/buffer
     time_key 60m
     chunk_size_limit 16m
  </buffer>
</match>